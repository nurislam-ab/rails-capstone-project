<p id="notice"><%= notice %></p>

<h2> Category name:
    <%= @category.name%>
</h2>

<h4> 
    Category priority <%= @category.priority%>
</h4>

<%= article_preview_by_category %>

<%= link_to 'Edit', edit_category_path(@category) %> |
<%= link_to 'Back', categories_path %>
